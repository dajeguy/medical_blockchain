extends layout2

block content
    div.text-center
        form(class="form-signin" method="POST" action="/login")
            #error
                if error
                    p.text-danger 登入發生錯誤!!

            - var h1Classes = ['h3', 'mb-3', 'font-weight-normal']
            h1(class=h1Classes) 醫療通資訊整合平台
            div.form-group
              label(for='selIdentity') 請選擇身份:
              select#selIdentity.form-control(type='select', placeholder='選擇身份' name='Identity' required='true' )
                option(value="Normal") 一般民眾
                option(value="Med") 醫護人員
            //-input account
            label( for="inputAccount" class="sr-only") 請輸入帳號
            input(type="text" name="username" id="inputAccount" class="form-control" placeholder="請輸入帳號" required autofocus)

            //-input password
            label(for="inputPassword" class="sr-only") 請輸入密碼
            input(type="password" name="password" id="inputPassword" class="form-control" placeholder="請輸入密碼" required)

            //-remember Checkbox
            - var divClasses=['checkbox', 'mb-3'];
            div(class=divClasses)
                label
                    input(type="checkbox" value="remember-me")
                    span &nbsp;記住我

            //-signIn button
            - var buttonClass=['btn', 'btn-lg', 'btn-primary', 'btn-block'];
            button(class=buttonClass type="submit") 登入

            - var pClasses=['mt-5', 'mb-3', 'text-muted'];
            p(class=pClasses) &copy; Medical blockchain 2022